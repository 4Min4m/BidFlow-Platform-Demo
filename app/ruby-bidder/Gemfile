source 'https://rubygems.org'
gem 'sinatra', '~> 4.0'
gem 'redis', '~> 5.0'
gem 'puma', '~> 6.0'
gem 'rackup', '~> 2.0'

# Distributed tracing (Phase 2) — instruments Sinatra + Redis automatically,
# exports spans via OTLP to the endpoint the Helm chart injects.
gem 'opentelemetry-sdk', '~> 1.4'
gem 'opentelemetry-exporter-otlp', '~> 0.29'
gem 'opentelemetry-instrumentation-all', '~> 0.65'
